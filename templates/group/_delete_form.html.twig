<form method="post" action="{{ path('app_group_delete', {'id': group.id}) }}" onsubmit="return confirm('Supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button class="btn">Supprimer</button>
</form>
