{% extends 'base.html.twig' %}

{% block title %}Ajouter une photo{% endblock %}

{% block body %}
    <h1>Ajouter une photo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Téléverser</button>
    {{ form_end(form) }}

    <a href="{{ path('app_vegetable_index') }}">Retour à la liste</a>
{% endblock %}
